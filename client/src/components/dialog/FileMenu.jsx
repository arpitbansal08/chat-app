import { Menu } from "@mui/material";
import React from "react";

const FileMenu = ({ anchorE1 }) => {
  return (
    <Menu open={false}>
      <div style={{ width: "10rem" }}>ksd</div>
    </Menu>
  );
};

export default FileMenu;
